package Section3;

abstract class MyAbstract {
	// we cannot create object from abstract class
	// abstract class can have abstract method as well not have any
	// if there is even one abstract method then we need to make the class abstract
	// abstract class meant to be inherited and its abstract method to be
	// implemented by the subclass(concrete class)
	// we cannot add static keyword to an abstract method nor to a constructor


	public abstract double getArea();

	
	public static void add	() {
		
	}
}
